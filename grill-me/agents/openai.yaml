interface:
  display_name: "Grill Me"
  short_description: "Stress-test a plan through structured decisions."
  default_prompt: "Use $grill-me to stress-test this plan until we reach shared understanding."
policy:
  allow_implicit_invocation: false
