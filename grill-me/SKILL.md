---
name: grill-me
description: Run an explicit, relentless interview that stress-tests a plan or design until shared understanding is reached. Use when the user invokes grill-me or directly asks to be grilled without requiring domain-document updates.
---

# Grill Me

Load and follow `$grilling` before asking any questions. Use it as the sole interview protocol for the session.

Do not add domain-document maintenance unless the user explicitly invokes a docs-backed workflow. Produce understanding and a decision closeout, not implementation.

If `$grilling` is unavailable, stop and tell the user to install `grill-me` together with `grilling`; do not reconstruct a partial protocol from memory.
