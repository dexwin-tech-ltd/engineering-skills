interface:
  display_name: "Code Review"
  short_description: "Doctrine-backed, evidence-verified code review"
  default_prompt: "Use $code-review with $engineering-for-certainty and every required companion skill to perform an evidence-verified review, report material findings first, and after I verify them, propose any warranted issue-review improvements."

policy:
  allow_implicit_invocation: true
