interface:
  display_name: "Engineering Observability"
  short_description: "Observability and logging doctrine"
  default_prompt: "Use $engineering-observability for logging, Better Stack, correlation IDs, and log-ingestion changes."

policy:
  allow_implicit_invocation: true
