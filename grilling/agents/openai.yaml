interface:
  display_name: "Grilling"
  short_description: "Resolve decisions through thorough, structured rounds."
  default_prompt: "Use $grilling to stress-test this plan with thorough, self-contained questions."
policy:
  allow_implicit_invocation: true
