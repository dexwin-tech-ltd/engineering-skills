interface:
  display_name: "Grilling"
  short_description: "Resolve decisions through dependency-aware rounds."
  default_prompt: "Use $grilling to stress-test this plan through dependency-aware rounds."
policy:
  allow_implicit_invocation: true
